# Images

Place your assets here:

| File | Used by | Notes |
|---|---|---|
| `sanket.jpg` | `index.html` hero portrait | Recommended: square crop, min 520×640px |
| `og.png` | Open Graph meta tag | 1200×630px |
| `favicon.png` | Browser tab | 32×32px |

After adding `sanket.jpg`, uncomment the `<img>` tag in the hero section of `index.html` and remove the placeholder `<div>`.
